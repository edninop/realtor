// Función para actualizar la fecha y hora function updateDateTime() { const now = new Date(); const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }; document.getElementById('date').textContent = now.toLocaleDateString('es-ES', options); document.getElementById('time').textContent = now.toLocaleTimeString('es-ES'); } // Función para obtener la ubicación del usuario function getLocation() { if (navigator.geolocation) { navigator.geolocation.getCurrentPosition(showPosition, showError); } else { document.getElementById("location").textContent = "Geolocalización no soportada."; } } function showPosition(position) { const lat = position.coords.latitude; const lon = position.coords.longitude; fetch(`https://nominatim.openstreetmap.org/reverse?format=json&lat=${lat}&lon=${lon}`) .then(response => response.json()) .then(data => { document.getElementById("location").textContent = `Ubicación: ${data.address.city || data.address.town || 'Desconocida'}, ${data.address.country}`; }) .catch(() => { document.getElementById("location").textContent = "No se pudo obtener la ubicación."; }); } function showError(error) { document.getElementById("location").textContent = "No se pudo obtener la ubicación."; } // Ejecutar las funciones al cargar la página updateDateTime(); setInterval(updateDateTime, 1000); // Actualizar la hora cada segundo getLocation();